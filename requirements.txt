# PyTorch + CUDA comes from Dockerfile (installed via cu121 wheels)
# DO NOT pin torch here; let Dockerfile handle it.

# Core
safetensors==0.4.3
numpy==1.26.4

# Diffusers stack
diffusers==0.30.0
transformers==4.41.2
accelerate==0.31.0
xformers==0.0.27.post2
Pillow==10.3.0
requests==2.32.2

# App
fastapi==0.110.2
uvicorn[standard]==0.29.0
boto3==1.34.131
