# Core runtime (torch is provided by the base image)
safetensors==0.4.3
numpy==1.26.4
Pillow==10.3.0
requests==2.31.0

# Diffusers stack (PyTorch 2.3.x & CUDA 12.1 compatible)
diffusers==0.29.0
transformers==4.40.2
accelerate==0.30.1

# API + AWS
fastapi==0.110.2
uvicorn[standard]==0.29.0
boto3==1.34.131
